//write a function that turns a string into a number. All inputs will be strings and every string will be a valid representation of an integral number

const stringToNumber = function(str){
  return Number(str)
}