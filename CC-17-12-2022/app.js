//Write a function that takes an array of strings and returns an array containing strings that are exactly 4 scharacters long. The original order should be kept for the output


function friend(friends){
    return friends.filter(names => names.length === 4)
   }